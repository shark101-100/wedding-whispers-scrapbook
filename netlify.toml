[build]
  command = "bun run build:netlify"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Bun используется для install/build:
  # Netlify сам подхватит bun.lock и поставит зависимости через bun.

# TanStack Start adapter для Netlify сам генерирует функции и редиректы.
